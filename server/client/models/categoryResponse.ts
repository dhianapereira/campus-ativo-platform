/**
 * Minimal stub for CategoryResponse to satisfy generated client imports.
 * Adjust via orval when the OpenAPI schema is fixed.
 */
export type CategoryResponse = {
  id?: string;
  name?: string;
  description?: string;
  isActive?: boolean;
};
